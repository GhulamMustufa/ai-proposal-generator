{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:ext": "playwright test extension.spec.ts --project=chromium --workers=1"
  },
  "devDependencies": {
    "@clerk/testing": "^1.2.3",
    "@playwright/test": "^1.45.0",
    "dotenv": "^16.4.5"
  }
}
